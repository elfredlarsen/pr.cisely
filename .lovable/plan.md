I `src/components/stopwatch/Stopwatch.tsx` erstattes de to konstanter:

```ts
const pauseBtn = `${baseBtn} bg-[#7c3aed] text-white`;
const resetBtn = `${baseBtn} bg-[#374151] text-white`;
```

Ingen andre konstanter, komponenter eller filer ændres.